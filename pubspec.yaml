name: hospital_app
description: A modern medical hospital app for humans and animals.
publish_to: 'none'

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  http: ^1.0.0
  shared_preferences: ^2.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/
